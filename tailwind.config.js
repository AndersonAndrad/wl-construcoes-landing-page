/** @type {import('tailwindcss').Config} */
export default {
    theme: {
        extend: {
            fontFamily: {
                outfit: ['Outfit', 'sans-serif']
            }
        }
    },
    plugins: []
};